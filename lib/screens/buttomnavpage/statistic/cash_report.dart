import 'package:flutter/material.dart';

class CashReport extends StatelessWidget {
  const CashReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Download Cash Flow Report'),
      ),
    );
  }
}