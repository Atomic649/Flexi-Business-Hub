import 'package:flutter/material.dart';

class OEMFactory extends StatelessWidget {
  const OEMFactory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OEM Factory'),
      ),
    );
  }
}