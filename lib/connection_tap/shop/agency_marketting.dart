import 'package:flutter/material.dart';

class AgencyMarketing extends StatelessWidget {
  const AgencyMarketing({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Agency Marketing'),
      ),
    );
  }
}